enum Role {
  USER
  ADMIN
  SUPER_ADMIN
}

enum UserStatus {
    BLOCKED
    DELETED
    ACTIVE
}

enum PricingType {
  FREE
  PREMIUM
}

enum ReviewStatus {
  PENDING
  APPROVED
  REJECTED
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
}

enum PaymentProvider {
  STRIPE
  PAYPAL
  RAZORPAY
}
enum MediaStatus {
  UPCOMING
  RELEASED
  ARCHIVED
}

enum PaymentStatus {
  PAID
  UNPAID
}