enum Role {
  USER
  ADMIN
}

enum UserStatus {
    BLOCKED
    DELETED
    ACTIVE
}

enum PricingType {
  FREE
  PREMIUM
}

enum ReviewStatus {
  PENDING
  APPROVED
  REJECTED
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
}

enum PaymentProvider {
  STRIPE
  PAYPAL
  RAZORPAY
}